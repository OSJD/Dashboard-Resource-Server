spring.application.name=${SPRING_APP_NAME:ResourceServer}
server.port=8083
logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:TRACE}

spring.config.import = application_prod.properties
spring.profiles.active = default

spring.datasource.url=jdbc:mysql://${DATABASE_HOST:192.168.1.213}:${DATABASE_PORT:3306}/${DATABASE_NAME:ht_sqlapi_db}
spring.datasource.username=${DATABASE_USERNAME:ht_sqlapi}
spring.datasource.password=${DATABASE_PASSWORD:W3bAp1Run$$}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:true}
#spring.jpa.hibernate.ddl-auto = update
logging.pattern.console = ${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}

server.servlet.session.timeout=${SESSION_TIMEOUT:20m}

spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${JWK_SET_URI:http://localhost:9000/oauth2/jwks}

spring.security.oauth2.resourceserver.opaque.introspection-uri= ${INTROSPECT_URI:http://localhost:9000/oauth2/introspect}
spring.security.oauth2.resourceserver.opaque.introspection-client-id=${INTROSPECT_ID:ht-client-opaque}
spring.security.oauth2.resourceserver.opaque.introspection-client-secret=${INTROSPECT_SECRET:VxubZgAXyyTq9lGjj3qGvWNsHtE4SqTqs}
